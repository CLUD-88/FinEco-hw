# HW02-2 复现说明

- **来源：** CSMAR 中山大学授权下载，2026-09-23 本地整理。原始文件位于 `FinEco-hw` 上一级目录的五个 CSMAR 解压文件夹中，文件夹前缀分别为 `上市公司基本信息年度表`、`中国上市公司股权性质文件`、`利润表`、两份 `资产负债表`（分别含主科目及短、长期借款）。原始字段说明在各文件夹的 `[DES][csv].txt`。
- **筛选条件：** 年度 2005—2015；当年行业名称含“房地产”；A 股代码以 0、3 或 6 开头；财务 `Typrep=A` 合并报表且报告期为 12 月 31 日；ROA、ROE 额外使用 2004 年末余额。产权按各年 `EquityNatureID` 分类。
- **本地衍生数据：** `data/panel_restricted.csv`（公司—年度面板）和 `data/sample_flow.csv`（筛选及合并流程）。它们均由 `code/prepare_data.py` 从原始文件生成，**属于受限数据，不得上传公开 GitHub 仓库或公开网盘**。如许可不允许向教师转交，请提供本说明、字段清单和处理代码，由具备 CSMAR 权限者按相同条件下载。
- **运行依赖：** Python 3.13；`pip install -r requirements.txt`。从本目录运行 `python code/prepare_data.py` 构建面板，再在本目录重启内核并运行 `hw02-2.ipynb` 的全部单元格。Notebook 使用相对路径 `data/`，预期得到 170 家公司、1,176 条公司—年度记录，包含公司数量、产权结构与六项指标图表。
- **口径限制：** `bankloan` 使用短期借款加长期借款除以总负债，可能包含非银行金融机构借款。此次下载的 `LISTINGSTATE` 说明不保证可精确识别历史每年年末退市状态；因此年度公司信息记录用作现有最接近的年度样本框架，并在 Notebook 中明确报告限制。

若在全新目录复现，请将以上五个原始解压文件夹放在 `FinEco-hw` 的上一级，保持表名和字段不变。若无法取得受限数据，只能阅读已执行 Notebook 中保留的输出，不能从零重跑第二题。
